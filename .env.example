# MindX 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ============================================
# 认证配置
# ============================================

# JWT密钥 (至少32字符，用于签名JWT token)
# 生成方法: openssl rand -base64 32
JWT_SECRET=your-32-character-secret-key-here-change-in-production

# 加密密钥 (用于加密API密钥和敏感数据)
# 生成方法: openssl rand -base64 32
ENCRYPTION_KEY=your-encryption-key-here-also-32-chars-min

# 数据库加密密钥 (可选，可与上面的加密密钥相同)
# 生成方法: openssl rand -base64 32
DB_ENCRYPTION_KEY=your-database-encryption-key-here

# ============================================
# 开发模式配置
# ============================================

# 开发模式开关 (开发时可设置为true)
BOT_DEV_MODE=false

# ============================================
# Ollama 配置
# ============================================

# Ollama服务地址
OLLAMA_BASE_URL=http://localhost:11434

# ============================================
# 日志配置
# ============================================

# 日志级别 (debug, info, warn, error)
LOG_LEVEL=info

# ============================================
# 其他配置
# ============================================

# 工作区目录 (默认: ~/.mindx)
# MINDX_WORKSPACE=/path/to/workspace

# 技能目录 (默认: ~/.mindx/skills)
# MINDX_SKILLS_DIR=/path/to/skills
