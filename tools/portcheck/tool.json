{
  "name": "portcheck",
  "description": "端口占用查询技能，查看指定端口的占用情况、进程信息",
  "version": "1.0.0",
  "type": "shell",
  "command": "portcheck_cli.sh",
  "parameters": {
    "port": {
      "type": "number",
      "description": "要查询的端口号，如 8080、3000",
      "required": true
    }
  },
  "timeout": 10
}