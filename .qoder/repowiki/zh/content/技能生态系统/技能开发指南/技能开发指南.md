# 技能开发指南

<cite>
**本文档引用的文件**
- [cmd/main.go](file://cmd/main.go)
- [internal/adapters/cli/skill.go](file://internal/adapters/cli/skill.go)
- [internal/usecase/skills/README.md](file://internal/usecase/skills/README.md)
- [internal/usecase/skills/SKILL_DEVELOPMENT.md](file://internal/usecase/skills/SKILL_DEVELOPMENT.md)
- [internal/usecase/skills/loader.go](file://internal/usecase/skills/loader.go)
- [internal/usecase/skills/executor.go](file://internal/usecase/skills/executor.go)
- [internal/entity/skill.go](file://internal/entity/skill.go)
- [internal/config/mcp.go](file://internal/config/mcp.go)
- [config/mcp_servers.json.template](file://config/mcp_servers.json.template)
- [internal/usecase/skills/mcp_manager.go](file://internal/usecase/skills/mcp_manager.go)
- [skills/calculator/SKILL.md](file://skills/calculator/SKILL.md)
- [skills/calculator/calculator_cli.py](file://skills/calculator/calculator_cli.py)
- [scripts/dev-start.sh](file://scripts/dev-start.sh)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)
10. [附录](#附录)

## 简介

MindX 是一个强大的技能驱动智能体平台，支持通过 CLI 技能和 MCP 技能两种方式扩展功能。本指南将详细介绍技能开发的标准规范和最佳实践，包括技能目录结构、配置文件格式和元数据定义。

## 项目结构

MindX 项目采用模块化的架构设计，主要包含以下核心目录：

```mermaid
graph TB
subgraph "项目根目录"
CMD[cmd/] -.- MAIN[main.go]
BIN[bin/] -.- MINDX[mindx]
DATA[data/] -.- MEMORY[memory/]
DIST[dist/] -.- RELEASES[releases/]
LOGS[logs/]
end
subgraph "核心模块"
INTERNAL[internal/] --> ADAPTERS[adapters/]
INTERNAL --> USECASE[usecase/]
INTERNAL --> ENTITY[entity/]
INTERNAL --> CONFIG[config/]
INTERNAL --> INFRASTRUCTURE[infrastructure/]
end
subgraph "技能系统"
SKILLS[skills/] --> CALCULATOR[calculator/]
SKILLS --> WEATHER[weather/]
SKILLS --> FILE_SEARCH[file_search/]
SKILLS --> OTHERS[其他技能...]
end
subgraph "配置系统"
CONFIG --> CAPABILITIES[capabilities.yml]
CONFIG --> CHANNELS[channels.yml]
CONFIG --> MODELS[models.yml]
CONFIG --> SERVER[server.yml]
end
```

**图表来源**
- [cmd/main.go](file://cmd/main.go#L1-L21)
- [internal/adapters/cli/skill.go](file://internal/adapters/cli/skill.go#L1-L50)

**章节来源**
- [cmd/main.go](file://cmd/main.go#L1-L21)
- [internal/adapters/cli/skill.go](file://internal/adapters/cli/skill.go#L1-L50)

## 核心组件

### 技能管理器 (SkillMgr)

技能管理器是 MindX 系统的核心组件，采用 Facade 设计模式，协调各个子组件完成复杂的技能管理任务：

```mermaid
classDiagram
class SkillMgr {
+LoadSkills() error
+Execute(name, params) string
+SearchSkills(query) []Skill
+ReIndex() error
+Enable(name) error
+Disable(name) error
}
class SkillLoader {
+LoadAll() error
+Load(name, path) error
+ParseSkillDef(data) SkillDef
+CheckDependencies(def) (bins, env)
}
class SkillExecutor {
+Execute(name, def, params) string
+ExecuteFunc(function) string
+UpdateStats(name, success, duration)
-buildCommand(def, params) Cmd
}
class SkillSearcher {
+Search(query) []Skill
-searchByVector(vector) []Skill
-searchByKeywords(keywords) []Skill
}
class SkillIndexer {
+ReIndex() error
+LoadFromStore() error
+GetVectors() [][]float64
-extractKeywords(def) []string
}
SkillMgr --> SkillLoader
SkillMgr --> SkillExecutor
SkillMgr --> SkillSearcher
SkillMgr --> SkillIndexer
```

**图表来源**
- [internal/usecase/skills/README.md](file://internal/usecase/skills/README.md#L11-L46)
- [internal/usecase/skills/loader.go](file://internal/usecase/skills/loader.go#L18-L33)
- [internal/usecase/skills/executor.go](file://internal/usecase/skills/executor.go#L19-L42)

### 技能定义实体

技能系统的核心数据结构包括技能定义、参数定义和技能信息：

```mermaid
classDiagram
class SkillDef {
+string name
+string description
+string version
+string category
+[]string tags
+[]string os
+bool enabled
+int timeout
+string command
+map[string]ParameterDef parameters
+Requires requires
+[]InstallMethod install
+string homepage
+map[string]interface{} metadata
+string output_format
+string guidance
+bool is_internal
}
class ParameterDef {
+string type
+string description
+bool required
}
class Requires {
+[]string bins
+[]string env
}
class InstallMethod {
+string id
+string kind
+string package
+string formula
+[]string bins
+string label
+[]string os
}
class SkillInfo {
+SkillDef def
+string directory
+string content
+bool canRun
+[]string missingBins
+[]string missingEnv
+string format
+string status
+[]float64 vector
+int successCount
+int errorCount
+time lastRunTime
+string lastError
+int64 avgExecutionMs
+[]int64 executionTimes
}
SkillDef --> ParameterDef
SkillDef --> Requires
SkillDef --> InstallMethod
SkillInfo --> SkillDef
```

**图表来源**
- [internal/entity/skill.go](file://internal/entity/skill.go#L5-L25)
- [internal/entity/skill.go](file://internal/entity/skill.go#L44-L82)

**章节来源**
- [internal/usecase/skills/README.md](file://internal/usecase/skills/README.md#L1-L168)
- [internal/entity/skill.go](file://internal/entity/skill.go#L1-L83)

## 架构概览

MindX 技能系统的整体架构采用分层设计，从底层的技能加载到上层的执行和搜索：

```mermaid
graph TB
subgraph "用户界面层"
WEB[Web Dashboard]
CLI[CLI Interface]
API[HTTP API]
end
subgraph "业务逻辑层"
SKILL_MGR[SkillMgr]
SEARCHER[SkillSearcher]
EXECUTOR[SkillExecutor]
LOADER[SkillLoader]
INDEXER[SkillIndexer]
end
subgraph "基础设施层"
MCP[MCP Manager]
EMBEDDING[Embedding Service]
OLLAMA[Ollama Service]
STORE[Persistence Store]
end
subgraph "外部系统"
LOCAL[Local Scripts]
MCP_SERVERS[MCP Servers]
EMBEDDING_API[Embedding APIs]
end
WEB --> SKILL_MGR
CLI --> SKILL_MGR
API --> SKILL_MGR
SKILL_MGR --> SEARCHER
SKILL_MGR --> EXECUTOR
SKILL_MGR --> LOADER
SKILL_MGR --> INDEXER
EXECUTOR --> MCP
INDEXER --> EMBEDDING
INDEXER --> OLLAMA
INDEXER --> STORE
MCP --> MCP_SERVERS
EMBEDDING --> EMBEDDING_API
LOCAL --> EXTERNAL[External Commands]
```

**图表来源**
- [internal/usecase/skills/README.md](file://internal/usecase/skills/README.md#L9-L46)
- [internal/usecase/skills/executor.go](file://internal/usecase/skills/executor.go#L57-L79)

## 详细组件分析

### CLI 技能开发

CLI 技能是最常见的技能类型，通过本地命令行脚本实现功能。

#### 目录结构规范

标准的 CLI 技能目录结构如下：

```
my-skill/
├── SKILL.md           # 技能定义文件（必需）
├── my-skill_cli.sh    # 命令行入口脚本
├── lib/               # 依赖库（可选）
└── references/        # 参考文档（可选）
    └── API_REFERENCE.md
```

#### SKILL.md 文件结构

SKILL.md 采用 YAML Frontmatter 和 Markdown 文档的组合格式：

```mermaid
flowchart TD
START[SKILL.md 文件开始] --> FRONTMATTER[YAML Frontmatter]
FRONTMATTER --> NAME[name 字段]
FRONTMATTER --> DESCRIPTION[description 字段]
FRONTMATTER --> VERSION[version 字段]
FRONTMATTER --> CATEGORY[category 字段]
FRONTMATTER --> TAGS[tags 字段]
FRONTMATTER --> ENABLED[enabled 字段]
FRONTMATTER --> OS[os 字段]
FRONTMATTER --> TIMEOUT[timeout 字段]
FRONTMATTER --> COMMAND[command 字段]
FRONTMATTER --> PARAMETERS[parameters 字段]
FRONTMATTER --> REQUIRES[requires 字段]
FRONTMATTER --> INSTALL[install 字段]
NAME --> MARKDOWN[Markdown 文档部分]
DESCRIPTION --> MARKDOWN
VERSION --> MARKDOWN
CATEGORY --> MARKDOWN
TAGS --> MARKDOWN
ENABLED --> MARKDOWN
OS --> MARKDOWN
TIMEOUT --> MARKDOWN
COMMAND --> MARKDOWN
PARAMETERS --> MARKDOWN
REQUIRES --> MARKDOWN
INSTALL --> MARKDOWN
MARKDOWN --> END[文件结束]
```

**图表来源**
- [internal/usecase/skills/SKILL_DEVELOPMENT.md](file://internal/usecase/skills/SKILL_DEVELOPMENT.md#L20-L49)

#### 参数传递机制

CLI 技能通过标准输入接收参数，采用 JSON 格式传递：

```mermaid
sequenceDiagram
participant CLI as CLI Interface
participant EXECUTOR as SkillExecutor
participant SCRIPT as Skill Script
participant STDOUT as Standard Output
CLI->>EXECUTOR : 执行技能请求
EXECUTOR->>EXECUTOR : 构建命令
EXECUTOR->>SCRIPT : 启动脚本进程
EXECUTOR->>SCRIPT : 写入JSON参数到stdin
SCRIPT->>SCRIPT : 解析JSON参数
SCRIPT->>STDOUT : 输出结果到stdout
STDOUT->>EXECUTOR : 读取输出
EXECUTOR->>CLI : 返回执行结果
```

**图表来源**
- [internal/usecase/skills/executor.go](file://internal/usecase/skills/executor.go#L138-L195)
- [internal/usecase/skills/executor.go](file://internal/usecase/skills/executor.go#L218-L260)

#### 错误处理机制

CLI 技能的错误处理遵循标准的 Unix 命令行约定：

```mermaid
flowchart TD
INPUT[接收输入参数] --> VALIDATE{验证参数}
VALIDATE --> |有效| EXECUTE[执行技能逻辑]
VALIDATE --> |无效| ERROR_OUTPUT[输出错误信息到stderr]
VALIDATE --> EXIT_CODE[设置非零退出码]
EXECUTE --> SUCCESS{执行成功?}
SUCCESS --> |是| OUTPUT_RESULT[输出结果到stdout]
SUCCESS --> |否| HANDLE_ERROR[捕获异常]
HANDLE_ERROR --> ERROR_OUTPUT
ERROR_OUTPUT --> EXIT_CODE
OUTPUT_RESULT --> NORMAL_EXIT[正常退出码]
```

**图表来源**
- [internal/usecase/skills/executor.go](file://internal/usecase/skills/executor.go#L179-L190)

### MCP 技能开发

MCP (Model Context Protocol) 技能提供了与外部工具和服务的无缝集成能力。

#### MCP 技能配置

MCP 技能通过 metadata 字段中的 mcp 配置进行标识：

```mermaid
classDiagram
class SkillDef {
+string name
+string description
+map[string]interface{} metadata
}
class MCPSkillMetadata {
+string server
+string tool
}
SkillDef --> MCPSkillMetadata : "metadata.mcp"
```

**图表来源**
- [internal/usecase/skills/SKILL_DEVELOPMENT.md](file://internal/usecase/skills/SKILL_DEVELOPMENT.md#L375-L416)

#### MCP 服务器配置

MCP 服务器配置支持两种传输方式：

```mermaid
graph LR
subgraph "MCP 服务器配置"
CONFIG[MCPServersConfig]
ENTRY[MCPServerEntry]
TYPE[传输类型]
end
subgraph "stdio 传输"
STDIO[本地子进程]
COMMAND[Command]
ARGS[Args]
ENV[Env]
end
subgraph "sse 传输"
SSE[远程HTTP SSE]
URL[URL]
HEADERS[Headers]
end
CONFIG --> ENTRY
ENTRY --> TYPE
TYPE --> |stdio| STDIO
TYPE --> |sse| SSE
STDIO --> COMMAND
STDIO --> ARGS
STDIO --> ENV
SSE --> URL
SSE --> HEADERS
```

**图表来源**
- [internal/config/mcp.go](file://internal/config/mcp.go#L13-L29)
- [internal/config/mcp.go](file://internal/config/mcp.go#L49-L64)

#### MCP 管理器工作流程

```mermaid
sequenceDiagram
participant APP as 应用程序
participant MCP_MGR as MCPManager
participant CLIENT as MCP客户端
participant SERVER as MCP服务器
participant SESSION as 会话
APP->>MCP_MGR : 连接服务器
MCP_MGR->>CLIENT : 创建MCP客户端
MCP_MGR->>SERVER : 建立传输连接
SERVER-->>MCP_MGR : 连接成功
APP->>MCP_MGR : 发现工具
MCP_MGR->>SESSION : ListTools请求
SESSION-->>MCP_MGR : 工具列表响应
MCP_MGR-->>APP : 返回工具信息
APP->>MCP_MGR : 调用工具
MCP_MGR->>SESSION : CallTool请求
SESSION-->>MCP_MGR : 工具执行结果
MCP_MGR-->>APP : 返回结果
```

**图表来源**
- [internal/usecase/skills/mcp_manager.go](file://internal/usecase/skills/mcp_manager.go#L49-L141)
- [internal/usecase/skills/mcp_manager.go](file://internal/usecase/skills/mcp_manager.go#L169-L204)

**章节来源**
- [internal/usecase/skills/SKILL_DEVELOPMENT.md](file://internal/usecase/skills/SKILL_DEVELOPMENT.md#L1-L452)
- [internal/config/mcp.go](file://internal/config/mcp.go#L1-L106)
- [internal/usecase/skills/mcp_manager.go](file://internal/usecase/skills/mcp_manager.go#L1-L292)

## 依赖关系分析

### 技能加载流程

```mermaid
flowchart TD
LOAD_ALL[LoadAll] --> READ_DIR[读取技能目录]
READ_DIR --> CHECK_DIR{检查是否为目录}
CHECK_DIR --> |是| LOAD_SKILL[Load技能]
CHECK_DIR --> |否| SKIP[跳过文件]
LOAD_SKILL --> READ_FILE[读取SKILL.md]
READ_FILE --> PARSE_DEF[解析技能定义]
PARSE_DEF --> CHECK_ENABLED{检查是否启用}
CHECK_ENABLED --> |否| SKIP_DISABLED[跳过禁用技能]
CHECK_ENABLED --> |是| CHECK_DEPS[检查依赖]
CHECK_DEPS --> DEPS_OK{依赖是否满足}
DEPS_OK --> |是| CREATE_SKILL[创建技能实例]
DEPS_OK --> |否| CREATE_ERROR[创建错误状态]
CREATE_SKILL --> SAVE_INFO[保存技能信息]
CREATE_ERROR --> SAVE_INFO
SAVE_INFO --> NEXT_SKILL[处理下一个技能]
```

**图表来源**
- [internal/usecase/skills/loader.go](file://internal/usecase/skills/loader.go#L35-L58)
- [internal/usecase/skills/loader.go](file://internal/usecase/skills/loader.go#L60-L123)

### 技能执行流程

```mermaid
flowchart TD
EXECUTE[Execute] --> CHECK_TYPE{检查技能类型}
CHECK_TYPE --> |内部技能| EXECUTE_INTERNAL[executeInternal]
CHECK_TYPE --> |MCP技能| EXECUTE_MCP[executeMCP]
CHECK_TYPE --> |外部脚本| EXECUTE_EXTERNAL[executeExternal]
EXECUTE_INTERNAL --> CALL_FUNC[调用内部函数]
CALL_FUNC --> UPDATE_STATS[更新统计信息]
UPDATE_STATS --> RETURN_SUCCESS[返回成功]
EXECUTE_MCP --> CONNECT_MCP[连接MCP服务器]
CONNECT_MCP --> CALL_TOOL[调用MCP工具]
CALL_TOOL --> UPDATE_STATS
UPDATE_STATS --> RETURN_SUCCESS
EXECUTE_EXTERNAL --> BUILD_CMD[构建执行命令]
BUILD_CMD --> SET_TIMEOUT[设置超时]
SET_TIMEOUT --> PREPARE_ENV[准备执行环境]
PREPARE_ENV --> RUN_CMD[执行命令]
RUN_CMD --> HANDLE_OUTPUT[处理输出]
HANDLE_OUTPUT --> UPDATE_STATS
UPDATE_STATS --> RETURN_SUCCESS
```

**图表来源**
- [internal/usecase/skills/executor.go](file://internal/usecase/skills/executor.go#L57-L79)
- [internal/usecase/skills/executor.go](file://internal/usecase/skills/executor.go#L81-L136)

**章节来源**
- [internal/usecase/skills/loader.go](file://internal/usecase/skills/loader.go#L1-L249)
- [internal/usecase/skills/executor.go](file://internal/usecase/skills/executor.go#L1-L402)

## 性能考虑

### 执行超时管理

技能执行支持超时控制，防止长时间阻塞：

- **默认超时**: 30秒
- **自定义超时**: 通过 `timeout` 字段配置
- **MCP 调用超时**: 支持独立的超时设置

### 统计信息收集

系统自动收集技能执行的统计信息：

- **成功率统计**: 成功/失败次数
- **执行时间监控**: 平均执行时间、最近执行时间
- **性能分析**: 最近100次执行时间记录

### 内存管理

- **并发安全**: 使用读写锁保护共享资源
- **资源清理**: 自动清理临时文件和进程
- **内存优化**: 限制统计信息存储数量

## 故障排除指南

### 常见问题诊断

#### 技能加载失败

**症状**: 技能无法加载或显示为不可运行

**排查步骤**:
1. 检查 SKILL.md 格式是否正确
2. 验证依赖项是否满足
3. 确认命令路径是否正确
4. 检查权限设置

#### 执行超时

**症状**: 技能执行超过设定时间

**解决方案**:
1. 增加 `timeout` 配置值
2. 优化脚本性能
3. 添加进度反馈机制

#### MCP 连接失败

**症状**: MCP 技能无法连接服务器

**排查步骤**:
1. 检查 MCP 服务器配置
2. 验证网络连接
3. 确认认证信息
4. 查看服务器状态

### 调试工具使用

#### 开发环境启动

使用提供的开发脚本启动完整的开发环境：

```bash
./scripts/dev-start.sh
```

该脚本会自动启动：
- 后端服务器 (端口 911)
- 前端开发服务器 (端口 5173)
- WebSocket 服务 (端口 1314)

#### CLI 调试命令

```bash
# 列出所有技能
mindx skill list

# 验证特定技能
mindx skill validate <技能名>

# 手动执行技能
mindx skill run <技能名> --参数值

# 重新加载技能
mindx skill reload
```

**章节来源**
- [scripts/dev-start.sh](file://scripts/dev-start.sh#L1-L285)
- [internal/adapters/cli/skill.go](file://internal/adapters/cli/skill.go#L24-L253)

## 结论

MindX 技能开发平台提供了完整的技能生态系统，支持本地 CLI 技能和 MCP 技能两种开发方式。通过标准化的配置文件格式和完善的执行框架，开发者可以快速创建、测试和部署各种类型的技能。

关键优势包括：
- **统一的开发规范**: 标准化的 SKILL.md 格式
- **灵活的执行方式**: 支持本地脚本和外部服务集成
- **完善的监控机制**: 自动统计和性能分析
- **便捷的开发工具**: 完整的 CLI 和调试工具链

## 附录

### 开发最佳实践

#### 目录结构建议
- 使用有意义的技能名称（小写字母和连字符）
- 合理组织文件结构，包含必要的文档
- 提供清晰的错误信息和帮助文档

#### 参数设计原则
- 明确参数类型和用途
- 提供合理的默认值
- 包含必要的验证逻辑
- 支持参数描述和示例

#### 错误处理规范
- 使用标准的 Unix 退出码
- 在 stderr 输出错误信息
- 提供有意义的错误描述
- 避免在 stdout 输出非结果内容

### 示例技能分析

#### 计算器技能示例

该示例展示了完整的 CLI 技能实现：

```mermaid
flowchart TD
START[技能启动] --> READ_PARAMS[读取JSON参数]
READ_PARAMS --> VALIDATE_PARAM{验证必需参数}
VALIDATE_PARAM --> |缺失| RETURN_ERROR[返回错误]
VALIDATE_PARAM --> |存在| CALCULATE[执行计算]
CALCULATE --> SAFE_EVAL[安全表达式求值]
SAFE_EVAL --> SUCCESS{计算成功?}
SUCCESS --> |是| FORMAT_RESULT[格式化结果]
SUCCESS --> |否| HANDLE_EXCEPTION[处理异常]
HANDLE_EXCEPTION --> RETURN_ERROR
FORMAT_RESULT --> PRINT_RESULT[打印JSON结果]
PRINT_RESULT --> EXIT_SUCCESS[正常退出]
RETURN_ERROR --> EXIT_ERROR[错误退出]
```

**图表来源**
- [skills/calculator/calculator_cli.py](file://skills/calculator/calculator_cli.py#L9-L39)

**章节来源**
- [skills/calculator/SKILL.md](file://skills/calculator/SKILL.md#L1-L37)
- [skills/calculator/calculator_cli.py](file://skills/calculator/calculator_cli.py#L1-L39)