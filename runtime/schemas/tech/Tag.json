{
  "type": "object",
  "description": "用于组织和过滤知识实体的标签、标记或类别标识",
  "properties": {
    "name": { "type": "string", "description": "标签的显示名称" },
    "color": {
      "type": "string",
      "description": "用于视觉识别的十六进制颜色代码（例如 '#FF5733'）"
    },
    "group": {
      "type": "string",
      "description": "标签所属的逻辑组或类别（例如 '语言'、'领域'、'难度'）"
    },
    "description": {
      "type": "string",
      "description": "对该标签表示什么以及何时应该使用的简要说明"
    },
    "related_tags": {
      "type": "array",
      "items": { "type": "string" },
      "description": "与该标签在语义上相关的其他标签名称"
    }
  },
  "required": ["name"]
}
