{
  "type": "object",
  "description": "在技术环境中遇到的问题、缺陷、故障或错误",
  "properties": {
    "name": {
      "type": "string",
      "description": "问题的简短识别名称或标题"
    },
    "description": {
      "type": "string",
      "description": "问题的详细描述，包括症状和上下文"
    },
    "severity": {
      "type": "string",
      "enum": ["严重", "主要", "次要"],
      "description": "问题的影响级别"
    },
    "status": {
      "type": "string",
      "description": "当前状态（例如打开、调查中、已解决、已关闭）"
    },
    "affected_component": {
      "type": "string",
      "description": "受问题影响的组件、模块或系统区域"
    },
    "workaround": {
      "type": "string",
      "description": "在永久修复完成之前的临时缓解措施或替代方案"
    }
  },
  "required": ["name", "description"]
}
