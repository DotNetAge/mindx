{
  "type": "object",
  "description": "Performance metric — yield, Sharpe ratio, ROI, volatility, alpha, beta",
  "properties": {
    "name": { "type": "string", "description": "Metric name" },
    "metric_type": {
      "type": "string",
      "enum": ["return", "ratio", "volatility"],
      "description": "Category of the metric"
    },
    "value": { "type": "number", "description": "Numeric value of the metric" },
    "benchmark": {
      "type": "string",
      "description": "Benchmark used for comparison (if applicable)"
    },
    "period": {
      "type": "string",
      "description": "Time period over which the metric is calculated (e.g., 1Y, YTD, 3M)"
    },
    "interpretation": {
      "type": "string",
      "description": "Brief interpretation or significance of the metric value"
    }
  },
  "required": ["name", "metric_type"]
}
