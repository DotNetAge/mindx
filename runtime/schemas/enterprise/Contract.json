{
  "type": "object",
  "description": "合同、协议、SLA、条款或政策，定义各方之间的约束性安排",
  "properties": {
    "name": {
      "type": "string",
      "description": "合同标题或参考名称"
    },
    "parties": {
      "type": "array",
      "items": { "type": "string" },
      "description": "合同中涉及的各方或签署方列表"
    },
    "type": {
      "type": "string",
      "description": "合同类型（如服务协议、保密协议、许可证、SLA、劳动合同）"
    },
    "effective_date": {
      "type": "string",
      "format": "date",
      "description": "合同生效日期（ISO 8601 格式）"
    },
    "expiry_date": {
      "type": "string",
      "format": "date",
      "description": "Date the contract expires or renews (ISO 8601 format)"
    },
    "status": {
      "type": "string",
      "enum": ["草稿", "生效中", "已过期", "已终止", "已续签"],
      "description": "合同当前的生命周期状态"
    },
    "key_terms": {
      "type": "array",
      "items": { "type": "string" },
      "description": "合同关键条款、条件或约定（如付款条款、终止通知）"
    }
  },
  "required": ["name", "parties"]
}
