{
  "type": "object",
  "description": "项目、计划、目标或里程碑，代表一个有定义目标的计划任务",
  "properties": {
    "name": { "type": "string", "description": "项目名称或代码名称" },
    "description": {
      "type": "string",
      "description": "项目范围和目标的简描述"
    },
    "owner": {
      "type": "string",
      "description": "项目负责人、赞助人或负责人"
    },
    "status": {
      "type": "string",
      "enum": ["已计划", "进行中", "已暂停", "已完成", "已取消"],
      "description": "项目当前的阶段或状态"
    },
    "start_date": {
      "type": "string",
      "format": "date",
      "description": "项目开始日期（ISO 8601 格式）"
    },
    "end_date": {
      "type": "string",
      "format": "date",
      "description": "项目结束或目标完成日期（ISO 8601 格式）"
    },
    "budget": {
      "type": "number",
      "description": "项目已批准的预算或已拨付的资金"
    }
  },
  "required": ["name"]
}
