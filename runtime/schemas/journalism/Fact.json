{
  "type": "object",
  "description": "A verified fact, data point, or statistic used in journalism",
  "properties": {
    "statement": {
      "type": "string",
      "description": "The factual statement or data point"
    },
    "category": {
      "type": "string",
      "description": "The type of factual information",
      "enum": ["data", "statistic", "verified_info", "historical", "legal"]
    },
    "source": {
      "type": "string",
      "description": "The original source or authority providing this fact"
    },
    "date_verified": {
      "type": "string",
      "description": "The date this fact was last verified (ISO 8601 format preferred)"
    },
    "methodology": {
      "type": "string",
      "description": "The method used to obtain or verify this fact, if applicable"
    },
    "related_claims": {
      "type": "array",
      "description": "References to claims that relate to this fact",
      "items": {
        "type": "string"
      }
    }
  },
  "required": ["statement", "category"]
}
