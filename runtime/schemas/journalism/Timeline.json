{
  "type": "object",
  "description": "与新闻报道相关的时间标记、事件序列或时间线",
  "properties": {
    "name": {
      "type": "string",
      "description": "时间线或事件序列的标签"
    },
    "entries": {
      "type": "array",
      "description": "时间线中按顺序排列的事件列表，每个事件包含日期和描述",
      "items": {
        "type": "object",
        "properties": {
          "date": {
            "type": "string",
            "description": "条目的日期或时间戳（优先使用 ISO 8601 格式）"
          },
          "description": {
            "type": "string",
            "description": "时间线中该时间点发生的事件"
          }
        },
        "required": ["date", "description"]
      }
    },
    "category": {
      "type": "string",
      "description": "该时间线涵盖的事件类别"
    },
    "source": {
      "type": "string",
      "description": "编制时间线信息的来源"
    }
  },
  "required": ["name", "entries"]
}
